{
  "name": "lesson-access-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:apply": "wrangler d1 execute lesson-access --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.14.1"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  }
}
